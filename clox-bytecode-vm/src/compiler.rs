use crate::chunk::Chunk;

pub fn compile(source: &str, chunk: &mut Chunk) -> Result<(), ()> {
    // TODO: The scanner and compiler are implemented in the next chapter.
    let _ = (source, chunk);
    Err(())
}
